import LoginForm from "./components/LoginForm";

const LoginPage = () => {
  return (
    <div className='h-screen bg-primary-bg flex justify-center items-center'>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
